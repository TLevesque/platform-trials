import { has } from "lodash-es";
import toto from "../helpers/serverHelpers";
import { qsdfg, wxcvb } from "../helpers/serverHelp";

console.log("lodash:", has(qsdfg, "a"));

export default () => (
  <div>
    <div>{toto}</div>
    <div>{console.log(qsdfg)}</div>
    <div>{wxcvb}</div>
  </div>
);
